<header class="navbar navbar-fixed-top navbar-info">
  <div class="navbar-inner">
    <%= link_to "DMEConstructor", root_path, id: "logo", style: 'margin-left:120px' %>
    <nav>
      <ul class="nav pull-right">
        <li>
          <% if signed_in? %>
              <%= link_to 'Sign out', signout_path %>
          <% else %>
              <%= link_to 'Sign in', signin_path %>
          <% end %>
        </li>
        <% if signed_in? %>
            <div class="btn-group">
              <div class="btn dropdown-toggle" data-toggle="dropdown" style="margin-right:120px">
                Admin
                <span class="caret"></span>
              </div>
              <ul class="dropdown-menu">
                <li><%= link_to 'Roles', '#' %></li>
                <li><%= link_to 'Users', users_path %></li>
                <li class="divider"></li>
                <li><%= link_to 'Connections', dme_connections_path %></li>
                <li><%= link_to 'Construction Tables', dme_tables_path %></li>
              </ul>
            </div>
        <% end %>
      </ul>
    </nav>
  </div>
</header>